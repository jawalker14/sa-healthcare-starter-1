# Phase 20 — Accessibility statement

## Context
Publish an accessibility statement and ensure it’s discoverable. Content is neutral and process-focused.

## Changes
- `content/pages/accessibility.mdx` (renders via dynamic content route)
- Added to sitemap.

## QA
1) Visit `/accessibility` → content renders with headings and lists.
2) Check metadata and canonical generated from the dynamic content page.

## Branching & PR
```bash
git checkout -b feature/20-a11y
git add -A
git commit -m "docs(a11y): add accessibility statement and sitemap entry"
git push -u origin feature/20-a11y
# Open PR into main using this doc as body
```
